// Metadata explorer page (cubes → dimensions → members). Implementation
// filled in by the pages feature work; stub exists for routing.
export function ExplorerPage() {
  return <div className="main-col page-pad">Explorer</div>;
}
