// Local data management page (export/import bundles, backups, disk usage).
// Implementation filled in by the data-management feature work; stub for routing.
export function DataPage() {
  return <div className="main-col page-pad">Data</div>;
}
